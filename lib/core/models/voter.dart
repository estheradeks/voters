class Voter {
  Voter({
    this.voterAddress,
    this.name,
    this.phone,
    this.isVerified,
    this.hasVoted,
    this.hasRegistered,
  });

  String voterAddress;
  String name;
  String phone;
  bool isVerified;
  bool hasVoted;
  bool hasRegistered;
}
